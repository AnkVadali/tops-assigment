//Q.12 Accept number of students from user. I need to give 5 apples to each student. how many apples are required
#include <stdio.h>

int main() {
    int n, i;
    printf("\nenter the number of students = ");
    scanf("%d", &n);
    i = 5 * n;
    printf("required number of apple = %d  ", i);
    

    
}